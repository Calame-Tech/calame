name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: [18, 20]

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Incremental TypeScript builds (refactor plan #23). Every package is a
      # `composite` project, so tsc validates the restored dist/.tsbuildinfo
      # itself and recompiles only what changed since the cached commit — a
      # stale cache is safe by construction. Key on the exact commit, restore
      # from the most recent previous build on any branch.
      - name: Restore incremental build cache
        uses: actions/cache@v4
        with:
          path: |
            packages/*/dist
            packages/*/tsconfig.tsbuildinfo
            ee/*/dist
            ee/*/tsconfig.tsbuildinfo
            tsconfig.tsbuildinfo
          key: ts-build-${{ runner.os }}-node${{ matrix.node-version }}-${{ github.sha }}
          restore-keys: |
            ts-build-${{ runner.os }}-node${{ matrix.node-version }}-

      - name: Build
        run: pnpm build

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm typecheck

      - name: Test with coverage
        run: pnpm test:coverage

      - name: Upload coverage report
        if: matrix.node-version == 20
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage/
          if-no-files-found: warn
          retention-days: 14

      - name: Audit dependencies
        run: pnpm audit --prod --audit-level=high

  format-check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check formatting
        run: pnpm format:check

  security-scan:
    runs-on: ubuntu-latest
    container:
      image: semgrep/semgrep

    steps:
      - uses: actions/checkout@v4

      - name: Security scan (semgrep)
        # `semgrep ci` needs a Semgrep Cloud token and `--fail-level` is not a
        # valid flag — that combination made this job error out. Use `scan` with
        # explicit rulesets; fail the build only on ERROR-severity findings.
        run: semgrep scan --config p/javascript --config p/owasp-top-ten --config p/security-audit --severity ERROR --error --metrics off
